---
defaultNamespace: cert-manager
kustomize:
  dir: clusters/${.ClusterName}
helm:
  repo: https://charts.bitnami.com/bitnami
  chart: cert-manager
  version: 1.3.17
  values:
    namespace: cert-manager
    installCRDs: true
